## 1. 官网轮换表解析

- [x] 1.1 在限行服务中加入固定的北京交管详情页备用来源，并实现共享的 HTML 轮换表解析入口；用页面 fixture 验证能够定位表头、日期区间和五个工作日尾号单元格
- [x] 1.2 将轮换区间展开为每日规则，解析并应用内嵌 `Holiday` 清单；用单元测试验证周末、节假日、日期边界和无效表格不会产生猜测规则
- [x] 1.3 统一页面规则的来源标记和现有 `TrafficRule`/缓存字段；用单元测试验证中文日期格式、尾号格式和“不限行”语义保持兼容

## 2. 两条获取路径接入

- [x] 2.1 在异步主流程中保持缓存和主 JSON API 优先，主来源失败或空响应时切换官网页面并复用现有缓存写入；用失败注入测试验证降级顺序和缓存结果
- [x] 2.2 在同步兼容流程中接入同一页面解析结果并转换为旧 `limitedTime`/`limitedNumber` 字典格式；用等价性测试验证同步与异步对相同日期返回相同尾号
- [x] 2.3 扩展服务状态或日志以区分主 JSON API、官网页面和缓存来源；用单元测试验证页面降级时来源可观测且不破坏既有状态字段

## 3. 回归验证

- [x] 3.1 运行 `pytest tests/unit/`，确认现有限行、缓存和兼容接口测试全绿，并补齐页面解析及两条降级路径的回归用例
- [x] 3.2 运行 `tox -e format`，确认 Black 无未提交格式差异；按项目约定在 push 前完成并提交格式化结果
- [x] 3.3 运行 `/review-loop` 审查实现；按审查结论修复 blocker/major 问题并重新审查，直到达到 APPROVE 或明确记录剩余风险
